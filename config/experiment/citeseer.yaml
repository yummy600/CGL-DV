# Experiment config for Citeseer dataset

data:
  dataset: "citeseer"
  num_neighbors: 2

model:
  hidden_dim: 256
  num_layers: 4

training:
  epochs: 100
  learning_rate: 1e-5

cgp:
  num_layers: 4  # Optimal for Citeseer
